name: Test Script
on:
  workflow_dispatch:

jobs:
  update:
    runs-on: windows-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
    - name: Run the scraper
      run: x.exe
